#include <bits/stdc++.h>

using namespace std;

//모든 세대 다 그림


int n, sx, sy, dir, gen; 
int dy[] = {0, -1, 0 ,1};
int dx[] = {1, 0, -1, 0};
 
  

int main()
{
	ios_base::sync_with_stdio(0); 
	cin.tie(0); cout.tie(0); 

	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> sx >> sy >> dir >> gen;

	}
}
